transformers==4.55.4
tokenizers>=0.21,<0.22  # transformers==4.55.4 yêu cầu bản này; nếu không pin,
                        # 1 gói khác có thể kéo tokenizers lên 0.22.x và làm
                        # sentence_transformers/transformers import lỗi
                        # (ImportError khi load HuggingFaceEmbeddings)
huggingface-hub>=0.34.0,<1.0  # transformers==4.55.4 yêu cầu bản này; nếu
                              # không pin, có thể bị kéo lên 1.x và cùng lỗi
                              # ImportError như tokenizers ở trên
peft==0.17.0
trl==0.21.0
accelerate==1.10.1
bitsandbytes==0.47.0
torch>=2.4,<2.8        # để tương thích bitsandbytes==0.47.0 + accelerate==1.10.1
sympy>=1.13.1          # torch>=2.4 cần sympy.printing hoạt động đúng; bản
                       # sympy cũ hơn/lazy-import thiếu sẽ gây
                       # "AttributeError: module 'sympy' has no attribute
                       # 'printing'" khi torch build FX graph (vd lúc
                       # model.generate() với 4-bit quantization)
datasets==4.0.0

# evaluate.py (RAGAs)
ragas==0.2.10
diskcache>=5.6.3      # ragas==0.2.10 cần gói này, không tự cài kèm (pip báo
                       # "dependency resolver" warning nếu thiếu)
pysbd                  # ragas/utils.py import trực tiếp pysbd.languages,
                       # không tự kéo theo khi cài ragas
appdirs
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38
langchain-huggingface  # HuggingFaceEmbeddings bản mới, không deprecated
                       # (thay cho langchain_community.embeddings.HuggingFaceEmbeddings)
sentence-transformers

# evaluate.py -- giám khảo qua API (ChatOpenAI-compatible). Chỉ thật sự cần
# khi đặt MEDQUAD_JUDGE_API_KEY; nếu không set, code fallback về Prometheus
# cục bộ (không cần package này) nhưng cứ cài sẵn cho tiện.
langchain-openai==0.1.23

# src/evaluation.py -- ROUGE/BLEU/perplexity + xuất CSV
evaluate==0.4.3
rouge_score==0.1.2   # bắt buộc cho evaluate.load("rouge")
nltk==3.9.1          # word_tokenize cho BLEU
pandas==2.2.2        # đọc/ghi CSV dự đoán